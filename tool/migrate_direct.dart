import 'dart:io';

// Migration removed: remote Google Sheets import has been deprecated.
// Use the CSV importer or re-add remote sync manually if needed.


Future<void> main() async {
  stdout.writeln('Remote Google Sheets migration removed. Use CSV importer or re-add migration if needed.');
  exit(0);
}
